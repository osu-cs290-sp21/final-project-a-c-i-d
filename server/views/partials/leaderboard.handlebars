<div class="leaderboard-dialog">
    <div class="modal-header">
        <h3><i class="fas fa-trophy"></i> Leaderboard <i class="fas fa-trophy"></i></h3>
    </div>
    <div class="leaderboard-modal-body">
        {{#each leaderboardData}}   {{!-- Move through each pair in leaderboardData.json --}}
            {{> leaderboardPlayer}} {{!-- leaderboard position template --}}
        {{/each}}
    </div>
</div>